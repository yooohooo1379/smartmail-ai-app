export default function ActionButtons() {
  return (
    <div
      style={{
        display: "flex",
        gap: "10px",
        marginTop: "20px",
      }}
    >
      <button>Summarize Email</button>

      <button>Generate Reply</button>
    </div>
  );
}